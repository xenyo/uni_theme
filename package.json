{
  "name": "uni_base",
  "private": true,
  "type": "module",
  "browserslist": "defaults",
  "scripts": {
    "watch": "nodemon --watch templates --ext mjs,scss --exec \"pnpm build\"",
    "build": "parcel build \"templates/**/[^_]*.{mjs,scss}\" --dist-dir ."
  },
  "devDependencies": {
    "@gatsbyjs/parcel-namer-relative-to-cwd": "^1.9.0",
    "@parcel/config-default": "^2.7.0",
    "@parcel/transformer-sass": "^2.7.0",
    "nodemon": "^2.0.20",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "@xenyo/sass-utils": "^1.3.0",
    "underscore": "^1.13.6"
  }
}
