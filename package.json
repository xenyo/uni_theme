{
  "name": "uni_base",
  "private": true,
  "type": "module",
  "browserslist": "defaults",
  "scripts": {
    "watch": "nodemon --watch templates --ext js,scss --exec \"pnpm build\"",
    "build": "rimraf dist && parcel build \"templates/**/[^_]*.{js,scss}\""
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "nodemon": "^2.0.20",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@xenyo/sass-utils": "^1.3.0",
    "underscore": "^1.13.6"
  }
}
