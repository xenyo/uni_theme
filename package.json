{
  "name": "uni_base",
  "private": true,
  "type": "module",
  "browserslist": "defaults",
  "scripts": {
    "watch": "rimraf dist && parcel watch \"templates/**/[^_]*.{js,scss}\"",
    "build": "rimraf dist && parcel build \"templates/**/[^_]*.{js,scss}\" --no-source-maps"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@xenyo/sass-utils": "^1.2.1"
  }
}
